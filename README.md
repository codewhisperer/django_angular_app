# django_angular_login
This is a simple app with a django backend and an angular front-end.
It allows you to create a user, log in and see a list of users.

To start the app do:

```
source start.sh
```
